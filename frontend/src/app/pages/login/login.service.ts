import { Injectable } from "@angular/core";

/**
 * LoginService - Servicio para lógica compartida del LoginComponent
 *
 * Nota: La lógica de autenticación principal está en AuthService
 * Este servicio es para funcionalidad específica del componente si fuera necesaria
 */
@Injectable({
  providedIn: "root",
})
export class LoginService {
  constructor() {}

  /**
   * Puedes agregar métodos específicos del login aquí
   * Ejemplo: validar formato de email, verificar si email existe, etc.
   */
}
